name: GraphQL Inspector

on:
  push:
    paths: [ "**.graphql" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: kamilkisiela/graphql-inspector@v3.4.0
        with:
          schema: "main:pmtraders/graphql/schema.graphql"
